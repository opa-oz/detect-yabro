{
  "name": "root",
  "private": true,
  "devDependencies": {
    "cz-lerna-changelog": "^2.0.3",
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  },
  "dependencies": {
    "global": "^4.4.0"
  },
  "scripts": {
    "build": "lerna run build --npm-client=yarn",
    "build:package": "tsc -b ./tsconfig.base.json",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "release": "lerna publish"
  }
}
